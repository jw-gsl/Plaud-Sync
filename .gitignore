.DS_Store
node_modules
/build
/.svelte-kit
/package
/.playwright-mcp/
.env
.env.*
!.env.example

# Local code-signing secrets — the .p8 key and filled-in signing.env live here.
# Never committed; this is your machine only.
/.signing/
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Built installers (rebuild with ./build-installer.sh)
/Plaud Sync.app
/*.dmg
/*.msi
/*.exe
/Plaud-Sync-macOS.zip

# build-installer.sh output (binaries; CI rebuilds these)
/dist/
